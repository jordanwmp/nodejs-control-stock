<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Stock Manager</title>
    <link rel="stylesheet" href="/css/styles.css" />
    <link rel="shortcut icon" href="/images/favicon.ico" type="image/x-icon">
    <!-- Option 1: Include in HTML -->
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.3.0/font/bootstrap-icons.css">
</head>

<body>
    <nav class="navbar">
        <a class="navbar__logo">
            <img src="/images/logo.png" alt="Node.js Stock Manager" />
        </a>
        <ul class="navbar__list">

            {{#if session.userid}}
                <li class="navbar__item">
                    <a class="navbar__link" href="/products/all">Products</a>
                </li>
                <li class="navbar__item">
                    <a class="navbar__link" href="/products/add">Add Product</a>
                </li>
                <li class="navbar__item">
                    <a class="navbar__link" href="/auth/logout">Logout</a>
                </li>
            {{else}}
                <li class="navbar__item">
                    <a class="navbar__link" href="/auth/register">Sign up</a>
                </li>
                <li class="navbar__item">
                    <a class="navbar__link" href="/auth/login">Sign in</a>
                </li>
            {{/if}}
        </ul>
    </nav>

    <div class="container">
        {{#if messages.message}}
        <div class="message">
            {{messages.message}}
        </div>
        {{/if}}
        {{#if message}}
        <div class="message">
            {{message}}
        </div>
        {{/if}}
        {{{body}}}
    </div>

    <footer>
        <span>&copy; NodeStock</span>
    </footer>


</body>

</html>